# Отчёт по анализу парсеров Tendrix

**Дата:** 12 февраля 2026  
**Цель:** Выяснить, почему парсеры ЭТП (Сбербанк-АСТ, Росэлторг, РТС и др.) возвращают 0 тендеров.

---

## 1. Сводка

| Парсер | Статус | Причина |
|--------|--------|---------|
| **ZakupkiContractParser** (ЕИС) | ✅ Работает | HTML-поиск по реестру контрактов |
| **SberbankAstParser** | ❌ 0 результатов | RSS не содержит поле ЭТП |
| **RoseltorgEisParser** | ❌ 0 результатов | RSS не содержит поле ЭТП |
| **RtsTenderEisParser** | ❌ 0 результатов | RSS не содержит поле ЭТП |
| **EtpGpbEisParser** | ❌ 0 результатов | RSS не содержит поле ЭТП |
| **EtpEtsEisParser** | ❌ 0 результатов | RSS не содержит поле ЭТП |
| **LotOnlineEisParser** | ❌ 0 результатов | RSS не содержит поле ЭТП |
| **ZakazRfEisParser** | ❌ 0 результатов | RSS не содержит поле ЭТП |
| **TekTorgEisParser** | ❌ 0 результатов | RSS не содержит поле ЭТП |
| **External parsers** | ⚠️ Частично | ~88 записей, блокировки на части сайтов |

---

## 2. Архитектура парсеров

### 2.1 Основной парсер ЕИС (ZakupkiContractParser)

- **URL:** `https://zakupki.gov.ru/epz/contract/search/results.html`
- **Метод:** HTML-скрапинг страницы поиска контрактов
- **Результат:** ~430 000+ записей в БД
- **Статус:** Работает стабильно

### 2.2 Парсеры ЭТП (8 штук)

Все используют единый подход:

1. **URL:** `https://zakupki.gov.ru/epz/order/extendedsearch/rss.html`
2. **Метод:** Запрос RSS с параметрами `searchString`, `fz44`, `fz223`
3. **Фильтрация:** Оставляют только те элементы, в `description` которых есть токены ЭТП (например, «Сбербанк-АСТ», «Росэлторг», «РТС-тендер»)
4. **Проблема:** В текущем формате RSS поле «Электронная площадка» **отсутствует** в `description`

---

## 3. Корневая причина: изменение формата RSS ЕИС

### 3.1 Проверка RSS

```python
# Запрос: 200 элементов RSS по запросу "закупка"
# Результат: 0 элементов содержат "Электронная площадка", "Сбербанк", "Росэлторг", "РТС"
```

**Структура RSS item (текущая):**
- `title` — название закупки
- `link` — ссылка на извещение
- `description` — HTML с полями: «Наименование объекта закупки», «Заказчик», «Начальная цена контракта», «ИКЗ» и т.п.
- `pubDate`, `author`

**Чего нет в description:**
- «Электронная площадка»
- «Наименование электронной площадки в информационно-телекоммуникационной сети «Интернет»»
- Любых упоминаний Сбербанк-АСТ, Росэлторг, РТС и т.д.

### 3.2 Где есть информация об ЭТП

Информация об электронной площадке присутствует **только на странице извещения** (`common-info.html`), а не в RSS. Для получения ЭТП по каждому тендеру потребовался бы отдельный HTTP-запрос на каждое извещение.

---

## 4. Детали по парсерам ЭТП

| Парсер | Токены для фильтрации | Ожидаемое поле в RSS |
|--------|----------------------|----------------------|
| SberbankAstParser | Сбербанк-АСТ, SBERBANK-AST.RU, sberbank-ast.ru | «Электронная площадка» |
| RoseltorgEisParser | РОСЭЛТОРГ, ROSELTORG.RU, ЕЭТП | «Электронная площадка» |
| RtsTenderEisParser | РТС-тендер, RTS-tender, rts-tender.ru | «Электронная площадка» |
| EtpGpbEisParser | etpgpb.ru, Газпромбанк, ЭТП ГПБ | «Электронная площадка» |
| EtpEtsEisParser | etp-ets.ru, Национальная электронная площадка | «Электронная площадка» |
| LotOnlineEisParser | ЛОТ-Онлайн, lot-online.ru, АО «РАД» | «Электронная площадка» |
| ZakazRfEisParser | ЗаказРФ, zakazrf.ru, АГЗ РТ | «Электронная площадка» |
| TekTorgEisParser | ТЭК-Торг, tektorg.ru | «Электронная площадка» |

Во всех случаях логика одинакова: парсеры ищут эти токены в `description` RSS, но в актуальном формате ЕИС этого поля там нет.

---

## 5. Внешние парсеры (external_parsers.py)

- **Метод:** Поиск URL через `robots.txt`/`sitemap.xml`, затем парсинг HTML страниц тендеров
- **Конфиг:** `external_sources.json` (26 источников)
- **Результат:** ~88 записей с `procurement_type = "Источник: {label}"`
- **Проблемы:**
  - Captcha и антибот на части сайтов
  - Разные структуры HTML
  - Ограничения по частоте запросов

---

## 6. Рекомендации по исправлению

### 6.1 Вариант A: Параметр фильтра ЭТП в RSS (предпочтительно)

Проверить, поддерживает ли RSS ЕИС параметр фильтрации по ЭТП (например, `etpId`, `epId` и т.п.). Если да — передавать его в URL и получать уже отфильтрованную выдачу.

**Действия:** Изучить форму расширенного поиска zakupki.gov.ru и сетевые запросы при выборе ЭТП.

### 6.2 Вариант B: Догрузка ЭТП со страницы извещения

Для каждого элемента RSS делать дополнительный запрос на `common-info.html` и извлекать название ЭТП.

**Минусы:** Рост нагрузки (1 запрос на тендер), риск блокировок, замедление синхронизации.

### 6.3 Вариант C: Переход на HTML-поиск заказов с фильтром ЭТП

Использовать `results.html` расширенного поиска заказов с выбранной ЭТП вместо RSS.

**Действия:** Найти параметры формы (в т.ч. для ЭТП), реализовать парсинг HTML-результатов.

### 6.4 Вариант D: Внешние парсеры как основной источник по ЭТП

Опираться на `external_parsers` для tektorg.ru, zakazrf.ru и др. Улучшить обход блокировок (прокси, задержки, смена User-Agent).

---

## 7. Выводы

1. **Основная причина** нулевых результатов по ЭТП — изменение формата RSS ЕИС: поле «Электронная площадка» больше не приходит в `description`.
2. **ZakupkiContractParser** работает, так как использует HTML-поиск по реестру контрактов, а не RSS заказов.
3. Все 8 парсеров ЭТП построены на устаревшем предположении о содержимом RSS и требуют переработки.
4. Внешние парсеры дают ограниченный объём данных, но остаются рабочим источником для части площадок.

---

## 8. Файлы для доработки

- `sberbank_ast_parser.py` — строки 235–239 (фильтр по `etp`)
- `roseltorg_parser.py` — строки 254–262
- `rts_tender_parser.py` — аналогичная логика
- `etpgpb_parser.py`, `etp_ets_parser.py`, `lot_online_parser.py`, `zakazrf_parser.py`, `tektorg_parser.py` — та же схема
